services:
  # mysql:
  #   image: mysql:8.1
  #   container_name: pwa_recipe_mysql
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     MYSQL_DATABASE: ${MYSQL_DATABASE}
  #     MYSQL_USER: ${MYSQL_USER}
  #     MYSQL_PASSWORD: ${MYSQL_PASSWORD}
  #   ports:
  #     - "33006:3306"
  #   volumes:
  #     - mysql-data:/var/lib/mysql

  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: pwa_recipe_backend
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     DB_HOST: mysql
  #     DB_NAME: ${MYSQL_DATABASE}
  #     DB_USER: ${MYSQL_USER}
  #     DB_PASSWORD: ${MYSQL_PASSWORD}
  #     DB_URL_PRISMA: "mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@mysql/${MYSQL_DATABASE}?schema=public"
  #   depends_on:
  #     - mysql
  #   volumes:
  #     - ./backend:/app
  #     - /app/node_modules

  react:
    build:
      context: ./react.js
      dockerfile: Dockerfile
    container_name: blog_frontend
    ports:
      - "5173:5173"
    # depends_on:
    #   - backend
    volumes:
      - ./react.js:/app
      - /app/node_modules

  # phpmyadmin:
  #   image: phpmyadmin:latest
  #   container_name: pwa_recipe_phpmyadmin
  #   depends_on:
  #     - mysql
  #   ports:
  #     - "8080:80"
  #   environment:
  #     PMA_HOST: mysql
  #     PMA_USER: ${MYSQL_USER}
  #     PMA_PASSWORD: ${MYSQL_PASSWORD}

# volumes:
#   mysql-data:
